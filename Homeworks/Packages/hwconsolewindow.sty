\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{hwconsolewindow}

\RequirePackage{minted}
\RequirePackage[minted]{tcolorbox}

\newcommand{\shellprompt}{>}
\newcommand{\shellcommand}[1]{\shellprompt \space \textbf{#1}}

\newtcblisting{consolewindow}[1][\normalsize]{
    listing engine=minted,
    title=>\_\null\hfill x,
    minted options={
        fontsize=#1,
        breaklines,
        breakanywhere,
        escapeinside=!!,
        breaksymbolleft=,
    },
    minted language=text,
    listing only,
    left skip=1mm,
    right skip=1mm,
    after skip=5mm,
    before skip=5mm,
    left=0mm,
    right=0mm,
    top=0mm,
    bottom=0mm,
    fonttitle=\ttfamily\normalsize,
    boxrule=0.02cm,
    colback=white,
    colframe=black,
    colbacktitle=black,
}
